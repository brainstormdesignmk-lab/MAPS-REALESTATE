# Supabase — the properties feed + backup source (FREEBUFF project, backup.ts)
SUPABASE_PROJECT_REF=euuaycmxfqiruspwjxhd
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Supabase — properties feed for geo-watcher (defaults to the live qkgio
# project baked into the scripts if unset)
# SUPABASE_URL=https://qkgioqotxjxffiaufgwd.supabase.co
# SUPABASE_ANON_KEY=

# SerpApi — monthly refresh top-up + queue-drain geocoding (STOPs at 20 left)
SERPAPI_KEY=

# DB paths (defaults: data/ under the repo root)
# DB_PATH=data/lina.db
# SKOPJE_POIS_DB=data/skopje-pois.db

# Backup hub (T60 role)
# BACKUP_DIR=/srv/backup
# BACKUP_PORT=8081

# capture-place-urls (headless Chrome, run on a machine WITH Chrome)
# CHROME_BIN=/usr/bin/chromium
# CAPTURE_DEBUG=1
